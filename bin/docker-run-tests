#!/usr/bin/env bash

TOPDIR="$(git rev-parse --show-toplevel 2>/dev/null)"
: ${TOPDIR:="$(cd $(dirname ${0})/.. && pwd)"}
: ${TOPDIR:?Failed to identify repository top level directory}

docker run getopts_long/kcov
